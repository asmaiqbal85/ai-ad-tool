{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "rewrites": [
    {
      "source": "/backend/:path*",
      "destination": "https://your-backend-url.com/api/:path*"
    }
  ]
}
